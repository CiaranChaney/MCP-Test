plugins {
    id "java"
}

allprojects {
    group = "com.example"
    version = "0.0.1"
}

subprojects {
    apply plugin: "java"

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    repositories {
        mavenCentral()
    }
}